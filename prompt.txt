Fix the chart resize handle in C:\Users\Administrator\Desktop\claude\task\frontend\src\pages\DashboardEditorPage.tsx.

Current problem: the resize handle uses @dnd-kit useDraggable with cumulative transform deltas, which makes the resize feel disconnected from the mouse. The chart grows uncontrollably instead of following the cursor precisely.

Fix: Replace the dnd-kit resize approach with native mouse events (onMouseDown/onMouseMove/onMouseUp). The resize should follow the cursor exactly — when the user drags the resize handle, the width/height should directly track the mouse position relative to the chart's initial top-left corner.

Steps:
1. Read C:\Users\Administrator\Desktop\claude\task\frontend\src\pages\DashboardEditorPage.tsx
2. In DraggableChart, remove the useDraggable for resize (keep the one for moving)
3. Remove the React.useEffect for resizeTransform
4. Add a native mousedown handler on the resize handle that:
   - Records the initial mouse position and initial chart size
   - Adds mousemove/mouseup listeners on document
   - On mousemove: calculates new width = initialWidth + (currentX - startX), new height = initialHeight + (currentY - startY), clamped to min 200x150
   - Calls onResize(chartId, newW, newH)
   - On mouseup: removes the listeners
5. The resize handle div should no longer have {...resizeAttrs} {...resizeListeners} or setResizeRef
6. Keep the stopPropagation on mousedown to prevent triggering the parent drag
7. Verify with: cd C:\Users\Administrator\Desktop\claude\task\frontend && npx tsc --noEmit
8. Report the result
