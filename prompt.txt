Fix the resize handle in C:\Users\Administrator\Desktop\claude\task\frontend\src\pages\DashboardEditorPage.tsx.

Current problem: The resize handle immediately starts following the mouse on click, and releasing doesn't reliably stop it. It should behave like a standard resize grip: mousedown to start, drag to resize, mouseup to stop.

Fix the handleResizeMouseDown function in DraggableChart to:

1. Use native DOM events (not React synthetic) for all listeners to ensure reliable mouseup capture
2. On mousedown on the resize handle:
   - Record startX, startY (e.clientX, e.clientY), startW, startH
   - Add document.addEventListener('mousemove', ...)  
   - Add document.addEventListener('mouseup', ...)
3. On mousemove: calculate newW = Math.max(200, startW + e.clientX - startX), newH = Math.max(150, startH + e.clientY - startY), call onResize
4. On mouseup: remove both event listeners
5. The resize handle div should use onMouseDown (native, not onPointerDown) with e.stopPropagation() and e.nativeEvent.stopImmediatePropagation() to prevent dnd-kit from intercepting
6. The handle function should take a native MouseEvent, not React.MouseEvent

Also add visual feedback: change cursor on the handle to 'nwse-resize'.

Verify: cd C:\Users\Administrator\Desktop\claude\task\frontend && npx tsc --noEmit
Report JSON result.
