你是 Claude Code，项目：C:\Users\Administrator\Desktop\claude\dashboard-builder
加载 superpowers:using-superpowers skill。

## 任务 1：修复前端构建错误

运行 `cd frontend && npx tsc --noEmit` 查看错误，然后逐个修复：

### 错误列表（修复方法：删除未使用的 import）

1. SqlQueryEditor.tsx:8 - `Title` 未使用 → 从 antd import 中移除 `Title`
2. DashboardEditorPage.tsx:1 - `useRef` 未使用 → 从 react import 中移除 `useRef`
3. DashboardEditorPage.tsx:3 - `Spin` 未使用 → 从 antd import 中移除 `Spin`
4. DashboardEditorPage.tsx:4 - `NumberOutlined` 未使用 → 从 icons import 中移除 `NumberOutlined`
5. DashboardEditorPage.tsx:358 - `isSelected` 未使用 → 找到这行，删除未使用的变量声明
6. DashboardListPage.tsx:2 - `Spin` 未使用 → 从 antd import 中移除 `Spin`
7. DataSourcePage.tsx:2 - `Modal` 未使用 → 从 antd import 中移除 `Modal`
8. DataSourcePage.tsx:14 - `modalOpen` 未使用 → 删除 useState 声明（如果 unused）

### 错误 9：DashboardEditorPage.tsx:178 类型错误

找到 line 178 附近的 setSelectedChart 调用，类型不匹配。修复方式：添加类型断言或确保返回值匹配 `ChartItem | null`。

可以先用 `read_file` 查看这行上下文再修复。

## 任务 2：安装 asyncpg

```bash
pip install asyncpg --proxy=http://127.0.0.1:7897
```

## 任务 3：验证

修复后运行：
- `cd frontend && npx tsc --noEmit` — 必须零错误
- `cd backend && python -m pytest tests/ -v` — 必须 36/36 通过

完成后汇报。
